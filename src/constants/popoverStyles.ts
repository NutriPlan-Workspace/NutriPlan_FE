import type { CSSProperties } from 'react';

export const NUTRITION_POPOVER_RADIUS = 34;

export const NUTRITION_POPOVER_BODY_STYLE: CSSProperties = {
  padding: 0,
  borderRadius: `${NUTRITION_POPOVER_RADIUS}px`,
  overflow: 'hidden',
};
